server:
  port: 0

spring:
  output:
    ansi:
      enabled: always
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    co.kr.clubvietnam: DEBUG
    org.springframework.security.web: DEBUG
    org.hibernate.type.descriptor.sql: TRACE